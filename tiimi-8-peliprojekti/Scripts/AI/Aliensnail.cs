using Godot;
using System;

public partial class Aliensnail : Alien
{
	public override void _PhysicsProcess(double delta)
	{
		if(GameManager.Instance.ActiveAquarium._objects.Count > 0)
		{
			foreach
		}

		base._PhysicsProcess(delta);
	}
}